[project]
name = "p0"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
dev-dependencies = [
  "hypothesis>=6.0",
]
